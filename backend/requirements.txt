# DriveVitals backend dependencies.
#
# Install with:
#     pip install -r backend/requirements.txt
#
# Versions are pinned to the known-good set used by CI and local
# development so Docker builds are deterministic.

fastapi==0.139.0
starlette==1.3.1
sqlalchemy==2.0.51
asyncpg==0.31.0
alembic==1.18.5
python-dotenv==1.2.2
uvicorn==0.49.0
websockets==16.0
