// Re-exports the shared layout shell so app-level code (App.jsx, and
// future route-level wrappers) has a single, stable import path.
export { default as AppLayout } from '../components/layout/AppLayout';
