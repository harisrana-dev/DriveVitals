services:
  postgres:
    image: postgres:16

    container_name: drivevitals-postgres

    restart: unless-stopped

    environment:
      POSTGRES_USER: drivevitals
      POSTGRES_PASSWORD: drivevitals123
      POSTGRES_DB: drivevitals

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: